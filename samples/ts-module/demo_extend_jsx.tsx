/**
 * - 无需任何额外依赖
 * - 使用 react-jsx
 * - typeRoots加入reect
 * - 试试看注释 import
 * - 把 import 藏到 reect 里去
 */
import 'reect/jsx-extend'

export default function App() {
  return <view bindtap={() => {}}>App</view>
}
