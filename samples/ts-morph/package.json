{
  "name": "ts-morph-samples",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "generate": "node ./scripts/parse.js",
    "clean": "node -e \"import('node:fs').then(fs=>fs.rmSync('results',{recursive:true,force:true})).then(()=>import('node:fs').then(fs=>fs.mkdirSync('results',{recursive:true})))\""
  },
  "dependencies": {
    "ts-morph": "^23.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
